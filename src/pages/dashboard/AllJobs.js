import { JobsContainer, SearchContainer } from '../../components';

const AllJob = (props) => {
  return (
      <>
        <SearchContainer/>
        <JobsContainer/>
      </>
  )
};

export default AllJob;
